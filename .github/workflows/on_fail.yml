name: Capture CI Failure

on:
  workflow_run:
    workflows: ["Engine CI"]
    types: [completed]

jobs:
  capture:
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest

    steps:
      - name: Download logs
        run: |
          echo "CI failed — logs will be captured by GitHub automatically"
